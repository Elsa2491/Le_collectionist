<h1><%= t("all_shops") %></h1>
<% @shops.each do |shop| %>
  <ul>
    <%= shop.name %>
    <%= link_to(
          t(
            "schedule.index.see_schedules",
          ), edit_shop_path(shop)) %>
  </ul>
<% end %>
